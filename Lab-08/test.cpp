/*for (char &i:s)


s,find_first_of ()

string to_lower (const string  &s){

}

struct comparer{
    bool operator(const string& a, const string &b) const{
        auto x = to_llower (a);
        auto y = to_lower (b);
        return x < y;

        auto = a.size();
        auto = b.size();
         
        return a.size() < b.size();
    }
};

auto = s.front()

auto = s.back();

map<string, int, comparer> v(my_compare);*/